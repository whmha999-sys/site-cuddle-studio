Problem identified: the image was added to `PromoReel`, but that section renders near the bottom of the homepage. The section you are seeing at the top is `LatestCreations`, so the image is not visible where you expected it.

Plan:
1. Move the uploaded T65 banner asset into the actual first visible homepage section (`LatestCreations`).
2. Remove or undo the T65 image from the lower `PromoReel` carousel if it should not appear there.
3. Keep the top row layout consistent with the existing banner cards so the new image appears immediately on page load.
4. Verify the homepage order so the T65 image is visible in the first section at the top.